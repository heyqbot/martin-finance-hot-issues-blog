<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{% if title %}{{ title }} · {% endif %}{{ site.title }}</title>
  <meta name="description" content="{{ site.description }}" />
  <meta name="keywords" content="금융, 재테크, 증시, 투자 인사이트, 데일리 뉴스" />
  <link rel="canonical" href="{{ site.url }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&family=Nanum+Gothic:wght@400;600&display=swap" rel="stylesheet" />
  <meta property="og:title" content="{{ site.title }}" />
  <meta property="og:description" content="{{ site.description }}" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="{{ site.url }}" />
  <meta property="og:site_name" content="{{ site.title }}" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:site" content="@martin" />
  <style>
    :root {
      font-family: 'Space Grotesk', 'Nanum Gothic', system-ui, sans-serif;
      background-color: #fffdf7;
      color: #111111;
      letter-spacing: -0.01em;
      scroll-behavior: smooth;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      background: #fffdf7;
    }
    .app-shell {
      max-width: 1080px;
      margin: 0 auto;
      padding: 36px 24px 60px;
    }
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 16px;
      padding-bottom: 24px;
      border-bottom: 1px solid rgba(0,0,0,0.08);
    }
    .brand-group h1 {
      margin: 0;
      font-size: clamp(2rem, 3vw, 2.6rem);
      font-weight: 600;
    }
    .tagline {
      margin: 4px 0 0;
      color: rgba(17,17,17,0.6);
      font-size: 0.95rem;
    }
    nav {
      display: flex;
      gap: 20px;
      font-size: 0.95rem;
    }
    nav a {
      color: #111111;
      text-decoration: none;
      font-weight: 500;
      padding-bottom: 4px;
      border-bottom: 2px solid transparent;
    }
    nav a.active,
    nav a:hover {
      border-color: #111111;
    }
    .hero {
      margin-top: 32px;
      background: #fdf9f2;
      border-radius: 24px;
      padding: 32px;
      border: 1px solid rgba(0,0,0,0.08);
      box-shadow: 0 22px 60px rgba(0,0,0,0.06);
      display: flex;
      justify-content: space-between;
      gap: 18px;
      flex-wrap: wrap;
    }
    .hero-content,
    .hero-info {
      flex: 1;
      min-width: 260px;
    }
    .hero-label {
      text-transform: uppercase;
      letter-spacing: 0.2em;
      font-size: 0.65rem;
      color: rgba(17,17,17,0.6);
      margin-bottom: 8px;
    }
    .hero h1 {
      margin: 0;
      font-size: clamp(1.8rem, 2.5vw, 2.4rem);
      color: #0e1225;
    }
    .hero-description {
      color: rgba(17,17,17,0.75);
      line-height: 1.7;
      margin: 12px 0;
    }
    .hero-meta {
      font-size: 0.9rem;
      color: rgba(17,17,17,0.6);
    }
    .hero-info p {
      margin: 0;
      color: rgba(17,17,17,0.7);
      line-height: 1.6;
    }
    .update-banner {
      margin-top: 20px;
      background: #ffffff;
      border-radius: 20px;
      padding: 24px 32px;
      border: 1px solid rgba(0,0,0,0.06);
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 18px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.05);
    }
    .update-banner strong {
      display: block;
      font-size: 1rem;
      margin-bottom: 6px;
      color: #0e1225;
    }
    .update-banner p {
      margin: 0;
      color: rgba(17,17,17,0.65);
      line-height: 1.7;
    }
    .layout-grid {
      margin-top: 32px;
      display: grid;
      grid-template-columns: minmax(0, 2.5fr) minmax(260px, 1fr);
      gap: clamp(28px, 3vw, 36px);
    }
    main {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    .issue-card {
      padding: 22px 26px;
      border-radius: 18px;
      border: 1px solid rgba(17,17,17,0.08);
      background: #ffffff;
      box-shadow: 0 16px 32px rgba(0,0,0,0.04);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .issue-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 22px 36px rgba(0,0,0,0.08);
    }
    .issue-header {
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 6px;
      font-size: 0.85rem;
      color: rgba(17,17,17,0.6);
      margin-bottom: 10px;
    }
    .issue-rank {
      font-weight: 600;
    }
    .issue-card h3 {
      margin: 0;
      font-size: clamp(1.6rem, 2vw, 1.95rem);
      line-height: 1.3;
    }
    .issue-summary {
      margin: 10px 0 4px;
      font-size: 1rem;
      line-height: 1.7;
      color: rgba(17,17,17,0.75);
    }
    .issue-card p {
      margin: 0;
      line-height: 1.7;
      color: rgba(17,17,17,0.8);
    }
    .issue-body {
      margin-top: 16px;
      color: rgba(17,17,17,0.82);
      line-height: 1.7;
    }
    .issue-source {
      margin: 16px 0 0;
      font-size: 0.85rem;
      color: rgba(17,17,17,0.6);
    }
    .tags {
      margin-top: 14px;
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
    .tag {
      background: rgba(17,17,17,0.08);
      border-radius: 999px;
      padding: 6px 12px;
      font-size: 0.8rem;
      color: #111111;
    }
    aside {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .ad-slot {
      padding: 18px;
      border-radius: 18px;
      border: 1px solid rgba(17,17,17,0.08);
      background: #ffffff;
      box-shadow: 0 16px 28px rgba(0,0,0,0.05);
    }
    .ad-slot p {
      margin: 0;
      color: rgba(17,17,17,0.7);
      line-height: 1.5;
    }
    aside section ul {
      padding-left: 18px;
      margin: 0;
      color: rgba(17,17,17,0.7);
      line-height: 1.6;
    }
    footer {
      margin-top: 46px;
      padding-top: 24px;
      border-top: 1px solid rgba(17,17,17,0.12);
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      gap: 12px;
      font-size: 0.85rem;
      color: rgba(17,17,17,0.6);
    }
    .privacy-links a {
      color: rgba(17,17,17,0.6);
      text-decoration: none;
    }
    @media (max-width: 900px) {
      .layout-grid { grid-template-columns: 1fr; }
      header { flex-direction: column; align-items: flex-start; }
      main, aside { order: unset; }
      nav { order: 2; }
    }
  </style>
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "NewsMediaOrganization",
      "name": "{{ site.title }}",
      "url": "{{ site.url }}",
      "description": "{{ site.description }}",
      "publisher": {
        "@type": "Organization",
        "name": "{{ site.title }}"
      }
    }
  </script>
</head>
<body>
  <div class="app-shell">
    <header>
      <div class="brand-group">
        <h1>{{ site.title }}</h1>
        <p class="tagline">{{ site.description }}</p>
      </div>
      <nav aria-label="Quick links">
        <a href="{{ site.url }}" class="active">홈</a>
        <a href="mailto:martin@example.com">문의</a>
        <a href="{{ site.url }}/privacy">프라이버시</a>
      </nav>
    </header>
    <section class="hero" aria-label="Hero spotlight">
      <div class="hero-content">
        <p class="hero-label">Daily Financial Hot Issues</p>
        <h1>핫한 글로벌 금융 흐름을 한 장으로</h1>
        <p class="hero-description">매일 오전 06:00 KST 글로벌 뉴스 가운데 가장 의미 있는 5건을 엄선해 핵심 요약, 시장 영향, 투자자 체크리스트로 정리합니다.</p>
        <p class="hero-meta">최근 업데이트: {{ today.now | readableDate }}</p>
      </div>
      <div class="hero-info">
        <p>Think-note·redbirdstock 스타일을 참고한 심플 카드 레이아웃과, AdSense 배너와 정보 블록을 분리한 구성으로 콘텐츠 집중도를 유지합니다.</p>
      </div>
    </section>
    <section class="update-banner" aria-label="Summary">
      <div>
        <strong>Quick snapshot</strong>
        <p>핵심 이슈 5개를 한 번에 전달하며, 모든 포스트에는 출처/태그/체크리스트가 포함되어 WHO, WHAT, WHY를 명확히 합니다.</p>
      </div>
      <div>
        <strong>AdSense-ready</strong>
        <p>광고 슬롯은 사이드바 상단에 고정해 콘텐츠 흐름을 방해하지 않도록 구성했습니다.</p>
      </div>
    </section>
    <div class="layout-grid">
      <main role="main">
        {{ content | safe }}
      </main>
      <aside role="complementary">
        <div class="ad-slot" aria-label="AdSense slot">
          <strong>AdSense slot (300x250)</strong>
          <p>수익화와 독자 경험의 균형을 맞춘 네이버형 레이아웃입니다.</p>
        </div>
        <section aria-labelledby="why-updated">
          <h3 id="why-updated">Daily signal tracker</h3>
          <ul>
            <li>금리 · 통화 · 정책 · 원자재 · 테크 시그널 태그 분류</li>
            <li>Cloudflare Pages 빌드 로그 → Telegram 알림</li>
            <li>핫 키워드 스크랩 · 추가 포스트 예정</li>
          </ul>
        </section>
      </aside>
    </div>
    <footer>
      <div>© {{ today.now | year }} {{ site.author }} · Cloudflare Pages · Eleventy</div>
      <div class="privacy-links">
        <a href="{{ site.url }}/privacy">Privacy</a>
        <a href="{{ site.url }}/disclaimer">Disclaimer</a>
      </div>
    </footer>
  </div>
</body>
</html>
